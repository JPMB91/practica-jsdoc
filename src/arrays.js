/**
 * Lista de edades de usuario
 * @type {Array}
 */
const age = [19,30, 20, 11];

/**
 * Puntuario de usuarios
 * @type {Array<Number>}
 */
const score = [10, 50, 55];


/**
 * Array de numeros y strings
 * @type {Array<(Number|String)>}
 */

const scoreAndStrings = [10, "hola", 69];
